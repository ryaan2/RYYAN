

const fastify = require ('fastify');

const route = {
    url: '/products',
    method: 'GET',
    handler: (request, reply) => {
        reply.send('products ctm');
    }
}


module.exports = route;
